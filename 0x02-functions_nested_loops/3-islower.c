#include "main.h"

/**
 * description: _islower - check for lower case letter
 * @c : character to check the case
 * Return:0 (success) or 1
 */

int _islower(int c)
{
	return (c >= 97 && c <= 122);

}

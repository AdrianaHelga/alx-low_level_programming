#include "main.h"
#include<stdio.h>
#include<ctype.h>

/**
 * _isupper: checks for uppercase letter
 * @c: characher to be checked
 * return (1): uppercase 
 * else return(0)
 */

int _isupper(int c)
{
return(c >= 65 && c <= 90);
}

#include "main.h"

/**
 * _isdigit: checks for uppercase letter
 * @c: characher to be checked
 * return (1): digit 
 * else return(0)
 */

int _isdigit(int c)
{
return(c >= 48 && c <= 57); /*specify ASCII digits*/
}
